{
  "default": {
    "paths": ["tests/features/*.feature"],
    "dryRun": false,
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "require": ["tests/stepDefinitions/*.js"]
  }
}
